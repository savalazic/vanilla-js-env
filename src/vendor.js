/* eslint-disable */
/*
  This is used by webpack in a production buily only, to create separate bundle. 
  Any files that aren't referenced here will be bundler into main.js for a production build.
*/

import fetch from 'whatwg-fetch';
